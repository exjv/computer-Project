# 第8步：工单字段补全与工单详情页完善

## 本步完成

1. 工单字段前后端继续打通：
   - 创建DTO新增：备注、原预计完成时间
   - 服务层创建时写入：备注、原预计完成时间、预计完成时间
   - 分配时补齐分配人信息（分配人ID/工号/姓名）
2. 工单附件联动：
   - 新增附件DTO
   - 新增工单附件列表接口
   - 新增工单附件新增接口（URL占位方式）
3. 工单详情页重构为业务详情页：
   - 基本信息区
   - 当前状态标签
   - 进度展示
   - 时间信息区
   - 附件图片区
   - 流程记录区
   - 可操作按钮区
4. 工单列表页提交表单补充关键字段：
   - 故障类型
   - 联系方式
   - 报修地点
   - 是否影响大范围网络
   - 原预计完成时间
   - 备注

## 本步接口变化

- `PUT /api/repair-orders/{id}/assign` 增加登录人上下文用于记录分配人
- `GET /api/repair-orders/{id}/attachments`
- `POST /api/repair-orders/{id}/attachments`

## 本步新增页面/模块

- `RepairOrderDetailView` 功能增强为完整详情页布局（含附件和可操作区）

## 本步待后续

- 下一步接入真实文件上传服务（当前附件为URL占位）
- 下一步补齐更多动作按钮和状态流转规则（延期申请、采购申请、关闭处理）
